{
  "name": "haren-monorepo",
  "version": "1.1.0",
  "private": true,
  "description": "Minimal skill-based agentic SDLC framework",
  "scripts": {
    "dev:cli": "pnpm --filter haren dev",
    "test": "pnpm -r test",
    "test:cli": "pnpm --filter haren test",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"apps/**/*.{ts,tsx}\" \"packages/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
